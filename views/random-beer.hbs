{{#each randomBeer}}

<div>
    <img src={{this.image_url}} alt="">
    <h3>{{this.name}}</h3>
    <p>{{this.tagline}}</p>
    <p>{{this.description}}</p>
    <ul>
        {{#each this.food_pairing}}
            <li> {{this}}</li>
        {{/each}}
    </ul>
</div>

{{/each}}